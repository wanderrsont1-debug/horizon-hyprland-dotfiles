# ~/.config/hypr/xdph.conf

# for things like OBS-STUDIO

screencopy {
    # Allows apps to request a "Restore Token" to skip the prompt next time.
    allow_token_by_default = true
    # 0 = no limit. Set to 60fps or 120fps to save laptop battery (screen sharing)
    max_fps = 60
}
